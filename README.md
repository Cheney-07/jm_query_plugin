# JM 编号查询插件（Meta Only）

## 功能
- 识别私聊中的 JM 编号
- 查询并返回基础元信息（标题）
- 不返回图片、不返回链接、不返回章节

## 风险说明
- 本插件依赖第三方镜像站
- 请仅用于学习与测试
- 强烈建议使用小号运行

## 配置
请在 `config.toml` 中填写有效的镜像地址：

```toml
[jm]
mirror = "https://your-mirror.example.com"
